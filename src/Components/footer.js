import React from 'react';
import {
  Box,
  Typography,
  IconButton
} from '@mui/material';

const Footer = () => {
  return (
    <Box sx={{ backgroundColor: 'darks.dark4', height: '15vh', p: 3 }}>
      <Typography variant='body2' sx={{ color: '#ffffff' }}>Test</Typography>
    </Box>
  )
}

export default Footer;